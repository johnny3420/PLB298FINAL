---
title: "Rachel_QTL_GWAS"
author: "Rachel Olson"
date: "March 7, 2020"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r, echo=FALSE}
#libraries
library(qtl)

#load
load("~/R/Classes/PLS298_QuantGen/PLB298FINAL/FinalProjectData.RData")
markers <- read.table("~/R/Classes/PLS298_QuantGen/PLB298FINAL/markers.txt", quote="\"", comment.char="")

#Add Chromosome row to G
colnames(markers) <- c("Chr","Code","Marker","A","B"); tmarkers <- t(markers); colnames(tmarkers) <- markers$Marker; tmarkerchr <- data.frame(tmarkers["Chr",]); colnames(tmarkerchr) <- "Chr"; markerchr <- t(tmarkerchr)
G <- rbind(markerchr, G)

write.csv(G, file = "G.csv", append = F, na = "NA", row.names = TRUE, col.names = TRUE)
write.csv(pheno, file = "pheno.csv", append = F, col.names = T, row.names = T, na = "NA")
crossobj <- read.cross(c("csv"), dir = "~/R/Classes/PLS298_QuantGen/PLB298FINAL/", genfile = "G.csv", phefile = "pheno.csv", )
```

QTL Mapping and Linkage

```{r, echo=FALSE}
```

GWAS

```{r, echo=FALSE}

```


